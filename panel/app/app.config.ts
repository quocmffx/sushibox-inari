export default defineAppConfig({
  ui: {
    // Brand accent = amber, matching the Inari logo (#f59e0b ≈ Tailwind amber-500).
    // One accent across the whole app (Color Consistency Lock).
    colors: {
      primary: 'amber',
      neutral: 'slate',
    },
  },
})
